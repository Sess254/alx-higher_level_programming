#!/usr/bin/python3
"""Defines an if the obj is excatly an instance of specified class"""


def is_kind_of_class(obj, a_class):
    """Returns true if obj is an instance or of instance  class"""
    return isinstance(obj, a_class)
